sentence = 'The quick brown fox jumped over the lazy dog.' #variole sentence with a string
sentence = sentence.replace('quick', 'slow') # reassigning quick with slow
print(sentence)

# replace function-replace and reassign a string value
# for reassignment gives you ability to reassign a value 
# first call sentence then .replace which takes 2 arguments (first word you are looking for then the one you want to replace)
# what happened above 
# just replace quick with slow using the replace function with the reassignment process

